global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "defect-detection-api"
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: "industrial-defect-detection"